# what is the main question:
goal: "тип меню"

# options for answers (goal should not be present):
options:
  цель:
    - "ввод данных большого объёма"
    - "сложный выбор"
    - "ввод данных"
    - "запрос"
    - "вычисление"
  тип пользователя:
    - "программист"
    - "опытный"
    - "неопытный"
  время обучения:
    - "больше недели"
    - "меньше недели"

# what are the rules (make sure they are consistent):
rules:
  -
    if:
      цель: ["ввод данных большого объёма", 0.8]
      тип пользователя: ["программист", 0.7]
    then:
      тип меню:
        - ["команды", 0.4]
        - ["вопрос-ответ", 0.6]
  -
    if:
      цель: ["ввод данных большого объёма", 0.8]
      тип пользователя: ["опытный", 0.8]
      время обучения: ["больше недели", 0.8]
    then:
      тип меню: ["заполнение экранной формы", 0.8]
  -
    if:
      цель: ["ввод данных большого объёма", 0.8]
      тип пользователя: ["опытный", 0.8]
      время обучения: ["меньше недели", 0.8]
    then:
      тип меню: ["заполнение экранной формы", 0.8]
  -
    if:
      цель: ["сложный выбор", 0.8]
      тип пользователя: ["программист", 0.8]
    then:
      тип меню: ["команды", 0.8]
  -
    if:
      цель: ["сложный выбор", 0.8]
      тип пользователя: ["опытный", 0.8]
      время обучения: ["больше недели", 0.8]
    then:
     тип меню:
      - ["заполнение экранной формы", 0.8]
      - ["вопрос-ответ", 0.8]
  -
    if:
      цель: ["сложный выбор", 0.8]
      тип пользователя: ["опытный", 0.8]
      время обучения: ["меньше недели", 0.8]
    then:
     тип меню: 
      - ["заполнение экранной формы", 0.8]
      - ["вопрос-ответ", 0.8]
  -
    if:
      цель: ["сложный выбор", 0.8]
      тип пользователя: ["неопытный", 0.8]
      время обучения: ["меньше недели", 0.8]
    then:
      тип меню: ["вопрос-ответ со справкой", 0.8]
  -
    if:
      цель: ["сложный выбор", 0.8]
      тип пользователя: ["неопытный", 0.8]
      время обучения: ["больше недели", 0.8]
    then:
      тип меню: ["вопрос-ответ со справкой", 0.8]
  -
    if:
      цель: ["ввод данных", 0.8]
      тип пользователя: ["программист", 0.8]
    then:
      тип меню: ["команды", 0.8]
  -
    if:
      цель: ["ввод данных", 0.8]
      тип пользователя: ["опытный", 0.8]
      время обучения: ["больше недели", 0.8]
    then:
     тип меню: 
      - ["заполнение экранной формы", 0.8]
      - ["вопрос-ответ", 0.8]
  -
    if:
      цель: ["ввод данных", 0.8]
      тип пользователя: ["опытный", 0.8]
      время обучения: ["меньше недели", 0.8]
    then:
     тип меню: 
      - ["заполнение экранной формы", 0.8]
      - ["вопрос-ответ", 0.8]
  -
    if:
      цель: ["ввод данных", 0.8]
      тип пользователя: ["неопытный", 0.8]
      время обучения: ["меньше недели", 0.8]
    then:
      тип меню: ["вопрос-ответ со справкой", 0.8]
  -
    if:
      цель: ["ввод данных", 0.8]
      тип пользователя: ["неопытный", 0.8]
      время обучения: ["больше недели", 0.8]
    then:
      тип меню: ["вопрос-ответ со справкой", 0.8]
  -
    if:
      цель: ["запрос", 0.8]
      тип пользователя: ["программист", 0.8]
    then:
      тип меню: 
      - ["команды", 0.8]
      - ["заполнение экранной формы", 0.8]
  -
    if:
      цель: ["запрос", 0.8]
      тип пользователя: ["неопытный", 0.8]
      время обучения: ["меньше недели", 0.8]
    then:
      тип меню: 
       - ["меню со справкой", 0.8]
       - ["вопрос-ответ со справкой", 0.8]
  -
    if:
      цель: ["запрос", 0.8]
      тип пользователя: ["неопытный", 0.8]
      время обучения: ["больше недели", 0.8]
    then:
      тип меню: 
       - ["меню со справкой", 0.8]
       - ["вопрос-ответ со справкой", 0.8]
  -
    if:
      цель: ["запрос", 0.8]
      тип пользователя: ["опытный", 0.8]
      время обучения: ["меньше недели", 0.8]
    then:
      тип меню: 
       - ["меню", 0.8]
       - ["вопрос-ответ", 0.8]
       - ["заполнение экранной формы", 0.8]
  -
    if:
      цель: ["запрос", 0.8]
      тип пользователя: ["опытный", 0.8]
      время обучения: ["больше недели", 0.8]
    then:
      тип меню: 
       - ["меню", 0.8]
       - ["вопрос-ответ", 0.8]
       - ["заполнение экранной формы", 0.8]
  -
    if:
      цель: ["вычисление", 0.8]
      тип пользователя: ["программист", 0.8]
    then:
      тип меню: 
      - ["команды", 0.8]
      - ["заполнение экранной формы", 0.8]
  -
    if:
      цель: ["вычисление", 0.8]
      тип пользователя: ["неопытный", 0.8]
      время обучения: ["меньше недели", 0.8]
    then:
      тип меню: 
       - ["меню со справкой", 0.8]
       - ["вопрос-ответ со справкой", 0.8]
  -
    if:
      цель: ["вычисление", 0.8]
      тип пользователя: ["неопытный", 0.8]
      время обучения: ["больше недели", 0.8]
    then:
      тип меню: 
       - ["меню со справкой", 0.8]
       - ["вопрос-ответ со справкой", 0.8]
  -
    if:
      цель: ["вычисление", 0.8]
      тип пользователя: ["опытный", 0.8]
      время обучения: ["меньше недели", 0.8]
    then:
      тип меню: 
       - ["меню", 0.8]
       - ["вопрос-ответ", 0.8]
       - ["заполнение экранной формы", 0.8]
  -
    if:
      цель: ["вычисление", 0.8]
      тип пользователя: ["опытный", 0.8]
      время обучения: ["больше недели", 0.8]
    then:
      тип меню: 
       - ["меню", 0.8]
       - ["вопрос-ответ", 0.8]
       - ["заполнение экранной формы", 0.8]